[pytest]
minversion = 7.0
addopts = -ra -q --durations=5
testpaths = tests
pythonpath = .
asyncio_mode = auto
markers =
    asyncio: mark a test as an asyncio coroutine
filterwarnings =
    ignore::DeprecationWarning
